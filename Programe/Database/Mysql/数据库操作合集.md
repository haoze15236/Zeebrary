# 字符集

```mysql
#查询数据库字符集
show variables like "%char%";
#查询字符排序规则
show variables like "%coll%";
#设置字符集
SET character_set_connection = "utf8";
```

# 排序规则

# 系统表

```mysql
//查看表大小
SELECT CONCAT(ROUND(SUM(DATA_LENGTH/1024/1024/90),10),'M') FROM information_schema.tables WHERE table_schema='xinchao_dev' AND table_name='gl_account_entry';
```

